//------------------------------------------
void nomal(){
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,180);
  servo_write(rightshoulderver,30);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,0);
  servo_write(leftshoulderver,150);
  servo_write(neck,90);
  delay(500);
}

//------------------------------------------
void righ_hand_up(){
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,150);
  servo_write(rightshoulderver,110);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,0);
  servo_write(leftshoulderver,150);
  servo_write(neck,90);
  delay(500);
}

//------------------------------------------
void left_hand_up(){
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,180);
  servo_write(rightshoulderver,30);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,30);
  servo_write(leftshoulderver,60);
  servo_write(neck,90);
  delay(500);
}

//------------------------------------------
void hands_up(){
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,60);
  servo_write(rightshoulderver,30);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,120);
  servo_write(leftshoulderver,150);
  servo_write(neck,90);
  delay(500);
}

//------------------------------------------
void aisatu(){
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,180);
  servo_write(rightshoulderver,0);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,0);
  servo_write(leftshoulderver,180);
  servo_write(neck,90);
  delay(500);
}

//------------------------------------------
void ohayou(){
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,60);
  servo_write(rightshoulderver,30);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,0);
  servo_write(leftshoulderver,150);
  servo_write(neck,90);
  delay(500);
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,180);
  servo_write(rightshoulderver,0);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,0);
  servo_write(leftshoulderver,180);
  servo_write(neck,90);
  delay(500);
}

//------------------------------------------
void konnitiha(){
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,60);
  servo_write(rightshoulderver,30);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,120);
  servo_write(leftshoulderver,150);
  servo_write(neck,90);
  delay(500);
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,180);
  servo_write(rightshoulderver,30);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,0);
  servo_write(leftshoulderver,150);
  servo_write(neck,90);
  delay(500);
}

//-----------------------------------------
void neck_shake(){
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,180);
  servo_write(rightshoulderver,30);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,0);
  servo_write(leftshoulderver,150);
  servo_write(neck,150);
  delay(500);
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,180);
  servo_write(rightshoulderver,30);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,0);
  servo_write(leftshoulderver,150);
  servo_write(neck,30);
  delay(500);
}

//------------------------------------------
void come(){
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,160);
  servo_write(rightshoulderver,110);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,20);
  servo_write(leftshoulderver,60);
  servo_write(neck,90);
  delay(500);
  servo_write(rightelbow,90);
  servo_write(rightshoulderside,160);
  servo_write(rightshoulderver,110);
  servo_write(leftelbow,90);
  servo_write(leftshoulderside,20);
  servo_write(leftshoulderver,60);
  servo_write(neck,90);
  delay(500);
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,160);
  servo_write(rightshoulderver,110);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,20);
  servo_write(leftshoulderver,60);
  servo_write(neck,90);
  delay(500);
  servo_write(rightelbow,90);
  servo_write(rightshoulderside,160);
  servo_write(rightshoulderver,110);
  servo_write(leftelbow,90);
  servo_write(leftshoulderside,20);
  servo_write(leftshoulderver,60);
  servo_write(neck,90);
  delay(500);
}

//------------------------------------------
void yosoro(){
  servo_write(rightelbow,180);
  servo_write(rightshoulderside,150);
  servo_write(rightshoulderver,140);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,0);
  servo_write(leftshoulderver,150);
  servo_write(neck,90);
  delay(500);
  servo_write(rightelbow,90);
  servo_write(rightshoulderside,150);
  servo_write(rightshoulderver,140);
  servo_write(leftelbow,0);
  servo_write(leftshoulderside,0);
  servo_write(leftshoulderver,150);
  servo_write(neck,90);
  delay(500);
  
}
